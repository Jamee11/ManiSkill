# EORT 思考与隐患

最后更新：2026-07-13 14:15:43 UTC

本文件约束 ManiSkill EORT 数据构建；所有后续数据、代码、环境和实验变更必须先核对下列未解决项。

| 状态 | 项目 | 证据与要求 | 影响范围 |
| --- | --- | --- | --- |
| 未解决 | Oracle 与部署条件不可混淆 | 首版只采 simulator GT，包含新增的 2D bbox/mask-centroid supervision，输出必须标为 oracle；不得将其视作真实感知输入。后续需单独做噪声、漏检和 ID-switch 测试。 | 所有 EORT 训练与结论 |
| 未解决 | 跨机器人动作语义 | 2026-07-16 实测将 `eef_transition_local(T,7)` 换帧后直接驱动 Panda 失败（TCP 位置 RMSE 8.65 cm、最大 11.08 cm），故它只保留为 observed dynamics。正式采集现先用官方 replay 生成成功的 root-frame normalized `pd_ee_delta_pose(T,7)`，sidecar/LeRobot 均独立标为 Panda controller command。Piper、真机 Franka、task-frame 与安全限制仍必须分别标定和 replay。 | DiT4DiT/X-WAM 训练与真机适配 |
| 未解决 | 未来信息泄露与离散 phase | `future_object_delta` 在 action 时刻不可观测，只能做 dynamics 辅助目标，不能作为可部署 action condition。PushCube v2 的 phase=3 已以原生 action-aligned success 为准；DiT4DiT 当前连续 condition 路径仍会 q99-normalize state，故不得将 interaction phase/visibility/role 直接塞入该路径，需先省略离散字段或实现 embedding/mask。 | 所有 oracle gate 与模型结论 |
| 未解决 | 坐标与四元数约定 | ManiSkill pose 四元数为 `wxyz`。v2 已用 `q_target ⊗ inverse(q_reference)` 导出 rotation-vector，并有 π/2 单元测试；新任务仍必须复用该约定，不能退回分量相减。 | v2 标签与后续任务 |
| 未解决 | 数据来源与成功性 | 只接受保持原生 motion-planning 行为的成功 `PushCubeEORT-v1` HDF5 episode；每条导出必须携带 source path、trajectory id、action、success 和 schema version。大规模 harness 额外写 `seed_manifest.json` 并用 disjoint seed blocks，但尚未完成目标 GPU 的 batch smoke。 | Pilot 与后续批量数据 |
| 未解决 | 可重现环境 | 独立 `maniskill-eort-v1` 环境必须记录 Python、Torch、SAPIEN、ManiSkill commit/version；环境 smoke 成功前不得采集批量数据。旧的未完成 `maniskill-eort` 目录不可覆盖或删除。 | 环境与实验 |
| 未解决 | 宿主渲染后端 | 本机 CPU renderer 在 SAPIEN 场景创建时崩溃（exit 139）；采集必须保留 GPU renderer，不能以 `CUDA_VISIBLE_DEVICES=''` 强制 CPU 回退。默认 GPU 0 已出现 Vulkan buffer OOM；必须用 `MANISKILL_EORT_CUDA_VISIBLE_DEVICES` 显式选择并 smoke 指定 GPU，不能自动抢占“最空闲”设备。 | 数据采集运行 |
| 未解决 | EORT 原始视觉分辨率 | `PushCubeEORT-v1` 的 1-trajectory GPU smoke、LeRobot exporter 和 DiT4DiT loader 均已验证 raw 256×256→model image `(3,224,224)` 的下采样链路；现有 128×128 pilot 不会被重写。后续批量采集前仍须在指定 GPU 上记录峰值显存和 object pixel 分布；不得由 128 上采样。 | 后续采集与视觉 tracker |
| 未解决 | 可见性分布与感知鲁棒性 | v2 已持久化 object/goal ID 并导出逐帧 visibility；新增 256px occlusion pilot 有 163/686 object-invisible 帧，且 exporter 已验证这些帧的连续 oracle condition 为零。它仍不覆盖真实 detector 的漏检/ID-switch、深度噪声或手/杂物遮挡；扩大采集前后均须报告可见率分布。 | 数据质量与 sim2real |
| 未解决 | 相机域随机化覆盖 | `PushCubeEORTCameraRand-v1` 已验证每 episode 固定、跨 episode 随机外参；它不等于遮挡、曝光、纹理、深度噪声或真机标定误差。批量数据必须按固定/随机相机 split 报告，并保留 raw sensor extrinsic/intrinsic。 | tracker 泛化与 sim2real |
| 未解决 | 遮挡标签泄露 | `PushCubeEORTOccluded-v1` 以 `occluder_active` 仅标注 simulator visual-only occluder 的存在；它不是部署可用观测，必须永远排除在 tracker/action 输入之外，仅用于 visibility 分层 QA。 | 所有遮挡实验 |
| 未解决 | PickCube 抓取阶段的可部署性 | `PickCubeEORT-v1` 的 `is_grasped` 与 `pick_interaction_phase` 由 simulator Panda 双指接触判定产生，只能作为训练/评测标签；真机必须以夹爪开度、力/电流和视觉 track 建立独立 grasp estimator，不能把该字段输入 policy。 | PickCube、Franka/Piper 迁移 |
| 未解决 | PickCube visual split 未经运行时 smoke | PickCube 已复用 PushCube 已验证的 per-episode camera-random 与 visual-only occlusion 代码，但尚未在指定 GPU 完成 reset/step、HDF5 持久化、可见率和原生规划成功率 smoke；在此之前不得批量采集或将其与 PushCube split 合并。 | PickCube 泛化数据 |
| 未解决 | X-WAM 权重与数据接口可用性 | 本地源码已确认 16D proprio/14D action 的双臂 mask 接口，但本地没有 `checkpoints/`。在获取并验证公开权重、RGB-D depth 编码、单臂 mask 和小规模 forward 前，不得把它作为已可运行基线。 | X-WAM 第二阶段主线 |
| 未解决 | 不覆盖已有数据 | 所有输出使用显式目录；默认拒绝已存在的输出路径，不删除现有数据。 | 数据安全 |

## 已解决项

| 日期 | 项目 | 证据 | 影响范围 |
| --- | --- | --- | --- |
| 2026-07-10 12:21:57 UTC | `maniskill-eort-v1` 环境 | Python 3.10.20、Torch 2.7.1+cu128、SAPIEN 3.0.3、ManiSkill 3.0.1；`sapien.Device('cpu')` 与 PushCube GPU-render smoke 已通过。 | 当前主机的 PushCube EORT 开发 |
| 2026-07-10 12:21:57 UTC | PushCube 时间对齐与成功来源 | 真实 smoke HDF5：action `(71,8)`、observation `(72,...)`、最终 success=True；sidecar 所有数值有限，且单元测试通过。 | `maniskill_push_cube_eort_oracle_v1` |
| 2026-07-11 03:45:45 UTC | PushCubeEORT-v1 真实物理信号 | CPU PhysX + GPU renderer 的 reset/step smoke 成功；`obj_linear_velocity`、`obj_angular_velocity`、`robot_obj_contact_force` 均为有限 `(1,3)`。 | v2 环境运行时字段 |
| 2026-07-13 06:50:18 UTC | v2 物理接触与时间对齐 | GPU 4 上真实成功 HDF5：`T=71` action、四项 raw 物理字段均为有限 `T+1=72`；derived 均逐元素等于 raw 前 `T` 帧，`physical_contact` 逐帧匹配 raw `robot_obj_contact_force_norm>1e-6`。未产生接触点或法线等伪标签。 | PushCubeEORT-v1 v2 的单轨迹验证 |
| 2026-07-13 10:00:29 UTC | object/goal segmentation 映射与 visibility QA | GPU 4 上真实成功 HDF5 保存静态 `(T+1,1)` object/goal actor ID；derived mask pixel count、fraction 与 visible bool 逐帧匹配 raw segmentation 前 `T` 帧。 | PushCubeEORT-v1 v2 的单轨迹 schema 验证 |
| 2026-07-13 12:37:00 UTC | DiT4DiT LeRobot v2 训练数据出口 | 专用 exporter 在真实 `T=71` v2 HDF5 上生成 Parquet/MP4/meta；`(71,8)` state、`(71,17)` current condition、`(71,7)` observed transition 与 raw/NPZ 逐元素对齐，future/phase 不进入 policy fields。 | DiT4DiT oracle gate 的接口验证 |
| 2026-07-13 13:05:00 UTC | segmentation-depth 几何观测 proxy | v2 导出器以毫米 depth、CV intrinsic/extrinsic 和 object actor-ID 回投可见表面 centroid；合成坐标单测与真实 `T=71` 导出均通过。它仍依赖 oracle segmentation ID，不能当作真实 tracker。 | 预测 object track 前的几何诊断 |
| 2026-07-13 13:33:54 UTC | 256×256 EORT raw-camera smoke | GPU 4 上 1/1 原生 motion-planning 成功；raw RGB/depth/segmentation 均为 `(72,256,256,3/1/1)`，derived 的 71/71 object frame visible 与 segdepth-valid，object mask 为 90–117 pixels。 | 后续高分辨率 EORT 采集 |
| 2026-07-16 15:18:24 UTC | PushCubeEORT state-only runtime | 补齐任务模块已有 `torch.stack` 所需的 import 后，`obs_mode=state_dict`、`control_mode=pd_ee_delta_pose` 环境可完成 reset 与 71-step replay；随后失败结果来自 action 语义而非环境构造。 | 仿真闭环 action QA |
