# EORT 思考与隐患

最后更新：2026-07-27 UTC

本文件约束 ManiSkill EORT 数据构建；所有后续数据、代码、环境和实验变更必须先核对下列未解决项。

| 状态 | 项目 | 证据与要求 | 影响范围 |
| --- | --- | --- | --- |
| 部分解决 | RSIG-v1 代码链路、target隔离与baseline可逆性 | 2026-07-28 已实现44D导出、60D训练/16D推理、6个Action token、Plan stop-gradient、optional zero-init Video residual及`RSIG_ENABLED=false`回退。完整Push/Pick train/val/test共1600条导出、文件审计和联合loader均通过；任意改变44D target不改变Action condition。尚未运行完整Cosmos forward/backward、checkpoint resume或闭环，因此不能把synthetic parity当作2B模型运行证据。 | RSIG-v1 数据、模型、训练与部署 |
| 未解决 | RSIG H18空间布局与live hook的真实checkpoint验证 | 代码按当前front+wrist 224×448合同要求H18 current slice为`14×28=392` tokens，并只在第二次future-video flow-matching forward的block-17 hook注入。2026-07-28 已加入不改变训练图的live hidden shape、Action专属投影梯度、Video专属`video_alpha`梯度及gate before/after诊断；仍必须在`Cosmos-Predict2.5-2B`上取得真实记录、确认front/wrist左右顺序、gate=0数值parity、adapter后续梯度和无额外Action侧2B计算图，不通过时先停训修接口。 | RSIG Video/Action共享表示 |
| 未解决 | RSIG auxiliary loss量级与40k稳定性 | role Gaussian CE、relation SmoothL1、contact/moving BCE、direction cosine目前各默认权重1；这是20-step量级观测起点，不是已校准的40k配置。2026-07-28 已新增四GPU 20-step专用launcher及ZeRO-safe梯度/gate/峰值显存诊断，但本机GPU仅余13-24.5GiB且利用率89-100%，未冒险启动。正式长训前必须记录各raw loss、梯度范数、`video_alpha`、显存、完整optimizer-step wall time并验证save/resume，再冻结一套与baseline同预算的权重；禁止只看total loss。 | RSIG训练策略与公平对比 |
| 未解决 | RSIG role peak不等于visibility概率 | rollout overlay显示softmax heatmap peak，仅用于定位置信度可视化，没有visibility calibration；EEF有效标签只检查正深度和画面范围，Object有效标签来自oracle mask。遮挡、漏检、ID-switch和真机检测失败仍需独立OOD评估，不能把peak标成部署visibility。 | RSIG感知评测与Sim2Real |
| 部分解决 | RSIG phase 语义、类别不平衡与部署边界 | 2026-07-27 最终 v1 规格删除共享 phase classifier、canonical phase exporter和任何 GT phase policy输入；Interaction Token只以 current relation/contact/object-moving及direction supervision约束。旧 phase保留为离线QA。该选择消除了 Push/Pick phase语义冲突，但也意味着v1不声称显式恢复完整task phase。 | RSIG-v1 数据、训练与主张 |
| 部分解决 | RSIG 单帧阶段歧义、memory不可训练与 EEF shortcut | 2026-07-27 确认当前 loader随机抽独立样本，v1彻底删除跨 episode memory；Approach/Release不可辨识性作为明确能力边界，不用oracle goal/phase修补。未来 EEF/Object仅预测 h=1/4/8 unit direction+moving probability，不回归magnitude/rotation/gripper，Plan→Action首版stop-gradient且必须做no-EEF-direction消融。 | RSIG-v1 模型、因果边界与消融 |
| 部分解决 | RSIG 精确 Goal Slot 不可部署 | 2026-07-27 最终 v1 删除 Goal Slot、goal pose/object-to-goal/goal heatmap loss；旧goal字段只留给baseline和离线QA。任务方向仅由预测的EEF/Object粗运动方向表示，静止帧通过moving mask不强迫猜方向。这避免不可见goal模板泄露，但复杂任务若视觉和语言均不提供目标信息仍不可辨识。 | RSIG-v1 数据、模型与Sim2Real |
| 未解决 | Push/Pick ID ceiling 无法支撑 RSIG 创新结论 | 现有 learned-tracker 40k 配对闭环已约为 Push 98%、Pick 98%；新模型即使有效也几乎没有 ID success-rate 提升空间。主结论必须依赖相机/遮挡/物理/distractor OOD 和更复杂多阶段任务，并与 robot-only、tracker-flat 做同预算比较。 | RSIG 实验设计与论文主张 |
| 未解决 | Oracle 与部署条件不可混淆 | 首版只采 simulator GT，包含新增的 2D bbox/mask-centroid supervision，输出必须标为 oracle；不得将其视作真实感知输入。后续需单独做噪声、漏检和 ID-switch 测试。 | 所有 EORT 训练与结论 |
| 未解决 | COE same-state restore 尚未证明 | HDF5 `env_states`/actor pose 不等于完整恢复 controller target、qpos/qvel、contact cache 和 solver runtime。任何 counterfactual branch 采集前，必须用相同 base state 重放 expert 8-step chunk，多次比较 TCP/object/contact/grasp；restore noise 必须显著低于 action-caused effect。未通过前不得声称 same-state intervention 或 causal effect。 | COE-WAM branch 数据与核心主张 |
| 未解决 | COE primary effect 与旧 dynamics 不同 | 当前 DiT4DiT 18D dynamics 是 observation-conditioned future object pose，不读取 candidate action；旧稿的 `Δee_to_object + Δobject_to_goal` 又会混入 EEF 运动学并在静态 goal 下重复 object displacement。第一版应以 `(K,3,3)` robot-base object displacement 加 contact/grasp event 为 primary target，relation 只作 current context/派生 scorer；future truth 继续隔离。 | COE-WAM 数据、head、loss 与评估 |
| 未解决 | COE candidate diversity 与公平上界 | 当前 Action DiT 随机采样不保证 K 个 chunk 产生不同 object outcome。写在线 selector 前必须用相同 K candidate set 比较 candidate-0、random、predicted 与 simulator-oracle best；若 oracle best 没有可测上界，不能靠增加 scorer 宣称改进。评估必须固定 simulator seed/model-noise，且 COE 与 compute-matched K=4 random 配对。 | COE-WAM candidate selection |
| 未解决 | 跨机器人动作语义 | `eef_transition_local` 直接驱动 Panda 已失败，只保留为 observed dynamics。replay controller action 现可精确解码为 metric task-axis `Δxyz(m)+Δrotvec(rad)+gripper_open_fraction`，71 步反编码误差 `1.49e-8` 且真实 Panda replay 1/1 success；原 normalized command 仍保留。2026-07-16 新增的离线预检只验证实测旋转、声明的逐步限幅和夹爪数值映射，并明确不连接硬件。本地 Franka 示例没有通用 `env.step` 驱动，UniVLA 节点绑定特定 `/mk1000`/IK/手眼标定，且未找到 Piper SDK；频率、workspace、碰撞、时延、急停和低速真机 replay 仍未解决。 | DiT4DiT/X-WAM 训练与真机适配 |
| 未解决 | 未来信息泄露与离散 phase | `future_object_delta` 在 action 时刻不可观测。默认 LeRobot 视图继续完全不暴露 future modality；显式 dynamics 视图只将 h=1/4/8 GT 作为 auxiliary target，DiT4DiT 在 predictor/action 前先清零全部 18D target+3D valid 槽位，并只把预测值送入 Action DiT。该隔离已有回归测试，但尚无训练证明 prediction 有效。interaction phase/role 等离散字段仍不得进入 q99 连续路径。 | 所有 oracle gate 与模型结论 |
| 未解决 | 坐标与四元数约定 | ManiSkill pose 四元数为 `wxyz`。v2 已用 `q_target ⊗ inverse(q_reference)` 导出 rotation-vector，并有 π/2 单元测试；新任务仍必须复用该约定，不能退回分量相减。 | v2 标签与后续任务 |
| 未解决 | 数据来源与成功性 | 只接受保持原生 motion-planning 行为的成功 EORT HDF5 episode；每条导出必须携带 source path、trajectory id、action、success 和 schema version。正式 collector 的成功筛选现有默认 `5×NUM_TRAJ` 尝试预算，耗尽后保留部分产物并报错；2026-07-21 起 controller replay 对每条最多额外重试 2 次，且在导出前核验 source/controller 条数与 terminal success，数量不足即保留诊断并停止。audit 会拒绝 exhausted/不完整 manifest。summary/audit 已报告 phase 分布并要求 phase 2/3；正式 preflight 还必须传 `500/100/200` per-variant 数量契约。真正的大规模成功率、phase 平衡、存储和长时稳定性仍待外机采集。 | Pilot 与后续批量数据 |
| 未解决 | 可重现环境 | production wrapper 已在每个完成 shard 写入 Python/ManiSkill/Torch/SAPIEN/NumPy/h5py 版本与 renderer GPU；audit 同时强制 controller commit、环境/控制/backend/seed provenance，并拒绝跨 shard 混用 commit 或 runtime。2026-07-20 已在本机 GPU 7 完成 1 条 PushCube v3 smoke（native/replay/sidecar/preview/LeRobot/audit 全通过），证明该设备当前驱动与显存可启动；1,600 条长时稳定性仍须以正式分片与最终 audit 验证。旧的未完成 `maniskill-eort` 目录不可覆盖或删除。 | 环境与实验 |
| 未解决 | 宿主渲染后端 | 本机 CPU renderer 在 SAPIEN 场景创建时崩溃（exit 139）；采集必须保留 GPU renderer，不能以 `CUDA_VISIBLE_DEVICES=''` 强制 CPU 回退。默认 GPU 0 已出现 Vulkan buffer OOM；正式 wrapper 自 2026-07-16 起在真实采集前强制非空 `MANISKILL_EORT_CUDA_VISIBLE_DEVICES`，但指定设备仍必须先 smoke，不能自动抢占“最空闲”设备。 | 数据采集运行 |
| 未解决 | EORT 原始视觉分辨率 | 三种 PushCube variant 的正式 harness smoke 均已验证 raw/QA 256×256→model image `(3,224,224)`；object mask 范围分别为 fixed 90–119、camera-rand 99–115、occluded 72–100 pixels。现有 128×128 pilot 不会被重写；正式大批量仍须记录峰值显存与完整 pixel 分布。 | 后续采集与视觉 tracker |
| 未解决 | 可见性分布与感知鲁棒性 | v2 已持久化 object/goal ID 并导出逐帧 visibility。Push occlusion pilot 有 163/686 object-invisible 帧；Pick 三分支 smoke 中 occluded seed 的 goal 为 0/72 可见，因而 segdepth proxy 72/72 均无效。这证明必须分别报告 object/goal/relational-valid，而不能只报 object visibility；仍未覆盖真实 detector 漏检/ID-switch、深度噪声或手/杂物遮挡。 | 数据质量与 sim2real |
| 未解决 | 相机域随机化覆盖 | `PushCubeEORTCameraRand-v1` 已验证每 episode 固定、跨 episode 随机外参；它不等于遮挡、曝光、纹理、深度噪声或真机标定误差。批量数据必须按固定/随机相机 split 报告，并保留 raw sensor extrinsic/intrinsic。 | tracker 泛化与 sim2real |
| 未解决 | 物体/物理域随机化覆盖 | 独立 Push/Pick `geometry_rand` 已通过 source→controller seeded reconstruction；实际 actor mass provenance 也已完整 replay 验证。summary/audit 现强制报告 extent/mass/friction 的 episode-level min/median/max，能发现采样塌缩。默认 fixed/camera/occluded 不变。现有少量 seed 仍不能估计成功率，且独立密度、颜色、光照、传感器噪声和真机分布标定未解决。 | 数据泛化与 sim2real |
| 未解决 | 遮挡标签泄露 | `PushCubeEORTOccluded-v1` 以 `occluder_active` 仅标注 simulator visual-only occluder 的存在；它不是部署可用观测，必须永远排除在 tracker/action 输入之外，仅用于 visibility 分层 QA。 | 所有遮挡实验 |
| 未解决 | PickCube 抓取阶段的可部署性 | `PickCubeEORT-v1` 的 `is_grasped` 与 `pick_interaction_phase` 由 simulator Panda 双指接触判定产生，只能作为训练/评测标签；真机必须以夹爪开度、力/电流和视觉 track 建立独立 grasp estimator，不能把该字段输入 policy。 | PickCube、Franka/Piper 迁移 |
| 未解决 | PickCube visual split 大规模分布未验证 | GPU 7 上 fixed/camera-rand/occluded 各 1 条的采集、成功 controller replay、HDF5/sidecar/LeRobot/loader 已通过，但单 seed 无法估计成功率与可见率分布；尤其 occluded goal 0/72 可见。正式批量前必须保留独立 QA，不能直接与 Push split 合并报告。 | PickCube 泛化数据 |
| 未解决 | X-WAM 权重与数据接口可用性 | 本地源码已确认 16D proprio/14D action 的双臂 mask 接口；官方已发布约 117 GB checkpoint，但本地仍没有 `checkpoints/`，且还需 Wan2.2-TI2V-5B 基座。在下载并验证 checksum、RGB-D depth 编码、单臂 mask 和小规模 forward 前，不得把它作为已可运行基线。 | X-WAM 第二阶段主线 |
| 部分解决 | Franka/Piper 任务可运行性筛选 | 2026-07-23：现有官方 Panda motion planner 已为 10 个单臂任务各保留 3 条成功 H.264 rollout；DrawSVG 缺依赖，DrawTriangle solver API 失配，其余 Panda 注册环境不等于已有成功 demo。当前仓库没有 Piper agent/URDF/controller/SDK；两个双臂环境均固定为双 Panda 且无 solver，因此不能用双 Panda 或随机视频冒充 Piper 能力。 | 后续任务选择、Piper 扩展与数据采集 |
| 部分解决 | learned-tracker policy 闭环证据 | 2026-07-22：20k checkpoint 在固定 50 seeds/task、8-step chunk、max 200 下为 Push `49/50=98%`、Pick `41/50=82%`。2026-07-24：完整 40k `bs3acc2` checkpoint 在相同规模协议下为 Push `49/50=98%`、Pick `48/50=96%`，tracker-valid 分别为 `3608/3608=100%`、`4416/4811=91.79%`，均零 action clipping。该强 ID baseline 带来 ceiling，COE 必须使用 paired model-noise、compute-matched K 候选和 hard/OOD；Action DiT 随机采样、真实相机/硬件闭环仍未解决，仿真成功率不得外推为 sim-to-real。 | tracker/policy 评估与部署结论 |
| 未解决 | 不覆盖已有数据 | 所有输出使用显式目录；单 shard collector 默认拒绝已有输出。矩阵重启只会跳过经当前 production audit 和数量契约验证完整的 job；任何部分/无效 job 都保留并停止，不做 HDF5 内续写或删除。 | 数据安全 |
| 已解决 | Sim2Real canonical frame 已显式化 | 2026-07-20：v3 raw 新增 `robot_base_pose`，sidecar 同时保留 world provenance 并导出 base-frame TCP/object/goal/relation/velocity/force/future/action；DiT4DiT exporter、tracker geometry 与 3-view data config 统一使用 robot-base。非 identity base（平移 + z 轴 90°）ManiSkill/DiT4DiT 回归测试均通过；真实 seed-1 smoke 的 loader 为 state `(1,64)`、action `(8,7)`、3 views × `(3,224,224)`。 | v3 sidecar、DiT4DiT exporter、真机 adapter |
| 未解决 | 三相机标定与图像预处理合同 | 用户指定 front 内参为 `/remote-home/share/teledata/raw/results_6_17_front_intrinsic`，cam2base 为 `/remote-home/share/teledata/raw/results_6_17_344522302193/cam2base_4x4.npy`。2026-07-20 进一步由 ROS topic、launch serial 和真机三视角样例确认：`344422300343/body_camera` 是右侧后方视角，`344522302193/front_camera` 是正前方，`339222070579/wrist_camera` 是腕部；DiT4DiT 现有 calibration manifest 确实颠倒了前两者。2026-07-22 simulator evaluator 已直接复用 v3 `front_camera`/`hand_camera`，真实 test seed 2000000 回放 52/52 tracker-valid，证明仿真训练/评估相机一致；这不解决真机合同。1280×720 真机内参映射到 256×256 仍须固定 undistort、crop、resize 顺序；wrist 的 intrinsics 与 hand-eye transform 尚未找到。未通过真机回投/重投影和可视化 smoke 前，不得宣称相机与真机匹配。 | Sim2Real front+wrist 采集与在线 tracker |
| 部分解决 | PushCube v3 目标区域构图 | 2026-07-20：仅 v3 将 cube→goal 中心距离从 0.20 m 改为 0.15 m；seed 2 native/controller 均 1/1 success，front/right goal 57/57 可见。目视仍发现 front 下缘和 right 右缘裁切，且单 seed 含相机/物体随机性，正式批量前仍需在目标机以多个 seed 检查 bbox 是否触边及任务步长分布。当前 smoke 由未提交工作树产生，metadata commit 仍为旧值，只可作 QA，不可训练。 | v3 PushCube 构图与数据分布 |
| 部分解决 | 停止人工遮挡训练后的 visibility 负样本 | 2026-07-20：v3 production collector 只有 `sim2real` 分支且显式 `artificial_occluder=false`；旧 occluded class/data 未删除。真实 seed-1 smoke 中 front/right object+goal 均 72/72 visible，wrist object 72/72、goal 33/72，证明自然负样本存在；但正式 500/100/200 分布仍未采集，visibility head 是否失衡仍须 audit。 | tracker 训练与 visibility calibration |
| 未解决 | RLBench GT-area 结论不可外推为可部署感知 | 2026-07-20 robustness 矩阵中 GT-area 对 `push_buttons` 提升显著，但 13D condition 含 simulator object center、progress 和 interaction state；difficulty summary 为 0 trials，原始日志也因 NumPy `int64` JSON 序列化失败。该实验只证明 oracle structured condition 对复杂任务有价值，不证明 RGB-D tracker 可恢复同等信息，也不支持直接以纯 action loss 联合训练 perception。 | learned tracker / joint-training 路线 |

## 已解决项

| 日期 | 项目 | 证据 | 影响范围 |
| --- | --- | --- | --- |
| 2026-07-20 14:34:08 UTC | v3 robot-base 三视角数据/模型入口 | GPU1 seed-1 真实链路 1/1 native + 1/1 controller success，72 steps；三路 raw RGB-D/segmentation 256×256、base-frame sidecar、三视角 overlay、3 MP4 LeRobot、DiT4DiT loader 和 v3 audit 均通过。桌面覆盖层 z-fighting 在首个 smoke 可视化发现并修正，修正后复采确认无条纹。 | ManiSkill v3 collection、tracker、DiT4DiT |
| 2026-07-17 03:07:31 UTC | safe matrix job restart | Synthetic fresh/complete/partial paths confirm 8 fresh jobs, audit-gated skipping of complete jobs, and fail-closed preservation of partial jobs. | 外机长任务重启 |
| 2026-07-17 02:57:44 UTC | finite successful-trajectory attempt budget | Production collector defaults to `5×NUM_TRAJ`, records attempted/successful/exhausted counts, and raises after retaining partial diagnostics. A real GPU4 seed-0 success completed within 1/1 attempt; focused audit rejects an exhausted manifest. | 外机长时间采集与不完整分片门控 |
| 2026-07-17 02:47:00 UTC | production episode-count contract | Audit parser/test accepts `train=500,val=100,test=200` and rejects a one-episode shard when 500 are required; default remains optional for smoke QA. | 正式训练前规模门控 |
| 2026-07-17 02:41:00 UTC | production matrix seed plan | Dry-run emits 8 jobs/5,200 episodes: each task visual train/val/test uses disjoint 0/1M/2M blocks and held-out geometry test uses 3M; metric action + future targets are explicit and execution is opt-in. | 外机完整采集排期 |
| 2026-07-17 02:37:00 UTC | shard-level object-physics QA | 合成 derivation/audit 通过；retained Push/Pick 三 seed 重导出分别报告 extent、mass、friction 的正值有序 min/median/max，episode count=3。 | 外机大规模物理覆盖审计 |
| 2026-07-17 02:34:00 UTC | actual actor mass provenance | GPU 4 Push seed 5100 与 Pick seed 6100 的 raw `obj_mass(T+1,1)` 分别为 `0.086086/0.072059 kg`，source/controller 逐元素相等并匹配体积×1000kg/m³；两条 replay、production audit 与 metric export 均通过。 | future-motion dynamics QA |
| 2026-07-17 02:25:00 UTC | Pick seeded geometry/friction replay | Pick geometry-rand seeds 4100–4102 的 source/controller extent+friction 逐元素相等；motion planning 与 controller replay 3/3 success，共 208 steps，production audit 与独立 metric DiT4DiT loader 通过。 | Pick 抓取物理域 held-out gate |
| 2026-07-16 19:41:52 UTC | seeded geometry/friction replay | Push geometry-rand seeds 3100–3102 的 source/controller extent+friction 逐元素相等；motion planning 与 controller replay 3/3 success，共 205 steps，production audit 与 metric LeRobot loader 通过。 | 独立物理域 held-out gate |
| 2026-07-16 19:26:21 UTC | object extent 持久化链路 | GPU 7 上 Push fixed 正式 wrapper 1/1 success；raw `obj_extent=(72,3)`、sidecar `object_extent=(1,3)` 均为约 0.04m，显式 provenance、三类 metric LeRobot 导出和 collection audit 通过。 | 后续 geometry randomization 的真值与 QA |
| 2026-07-10 12:21:57 UTC | `maniskill-eort-v1` 环境 | Python 3.10.20、Torch 2.7.1+cu128、SAPIEN 3.0.3、ManiSkill 3.0.1；`sapien.Device('cpu')` 与 PushCube GPU-render smoke 已通过。 | 当前主机的 PushCube EORT 开发 |
| 2026-07-10 12:21:57 UTC | PushCube 时间对齐与成功来源 | 真实 smoke HDF5：action `(71,8)`、observation `(72,...)`、最终 success=True；sidecar 所有数值有限，且单元测试通过。 | `maniskill_push_cube_eort_oracle_v1` |
| 2026-07-11 03:45:45 UTC | PushCubeEORT-v1 真实物理信号 | CPU PhysX + GPU renderer 的 reset/step smoke 成功；`obj_linear_velocity`、`obj_angular_velocity`、`robot_obj_contact_force` 均为有限 `(1,3)`。 | v2 环境运行时字段 |
| 2026-07-13 06:50:18 UTC | v2 物理接触与时间对齐 | GPU 4 上真实成功 HDF5：`T=71` action、四项 raw 物理字段均为有限 `T+1=72`；derived 均逐元素等于 raw 前 `T` 帧，`physical_contact` 逐帧匹配 raw `robot_obj_contact_force_norm>1e-6`。未产生接触点或法线等伪标签。 | PushCubeEORT-v1 v2 的单轨迹验证 |
| 2026-07-13 10:00:29 UTC | object/goal segmentation 映射与 visibility QA | GPU 4 上真实成功 HDF5 保存静态 `(T+1,1)` object/goal actor ID；derived mask pixel count、fraction 与 visible bool 逐帧匹配 raw segmentation 前 `T` 帧。 | PushCubeEORT-v1 v2 的单轨迹 schema 验证 |
| 2026-07-13 12:37:00 UTC | DiT4DiT LeRobot v2 训练数据出口 | 专用 exporter 在真实 `T=71` v2 HDF5 上生成 Parquet/MP4/meta；`(71,8)` state、`(71,17)` current condition、`(71,7)` observed transition 与 raw/NPZ 逐元素对齐，future/phase 不进入 policy fields。 | DiT4DiT oracle gate 的接口验证 |
| 2026-07-13 13:05:00 UTC | segmentation-depth 几何观测 proxy | v2 导出器以毫米 depth、CV intrinsic/extrinsic 和 object actor-ID 回投可见表面 centroid；合成坐标单测与真实 `T=71` 导出均通过。它仍依赖 oracle segmentation ID，不能当作真实 tracker。 | 预测 object track 前的几何诊断 |
| 2026-07-13 13:33:54 UTC | 256×256 EORT raw-camera smoke | GPU 4 上 1/1 原生 motion-planning 成功；raw RGB/depth/segmentation 均为 `(72,256,256,3/1/1)`，derived 的 71/71 object frame visible 与 segdepth-valid，object mask 为 90–117 pixels。 | 后续高分辨率 EORT 采集 |
| 2026-07-16 15:18:24 UTC | PushCubeEORT state-only runtime | 补齐任务模块已有 `torch.stack` 所需的 import 后，`obs_mode=state_dict`、`control_mode=pd_ee_delta_pose` 环境可完成 reset 与 71-step replay；随后失败结果来自 action 语义而非环境构造。 | 仿真闭环 action QA |
| 2026-07-16 15:58:07 UTC | DiT4DiT websocket client runtime dependencies | `maniskill-eort-v1` 新增并验证 `websockets==16.0`、`msgpack==1.1.2`，`websockets.sync.client` 可导入；未覆盖或删除旧环境。 | 训练后 Panda 闭环评测 |
| 2026-07-16 16:25:33 UTC | 三视觉分支正式 collection harness smoke | GPU 7 上 fixed/camera-rand/occluded 分别以 seed 0/100000/200000 成功采集并完成官方 `pd_ee_delta_pose` replay；controller HDF5/sidecar/9 个 LeRobot action 逐元素一致，三种 controller mixture loader 均输出 state `(1,64)`、action `(8,7)`、image `(3,224,224)`。 | PushCube 大规模采集启动前链路 |
| 2026-07-16 16:42:08 UTC | PickCube 三视觉分支运行时 smoke | GPU 7 上三分支以 seeds 0/100000/200000 成功采集与 controller replay，长度 74/81/72；raw `is_grasped` 与 derived phase=2 对齐，9 个 LeRobot action 与 HDF5 精确一致，三 controller mixture loader shape 通过。 | PickCube EORT 运行链路 |
| 2026-07-16 16:42:08 UTC | 自动 shard-level relational visibility QA | v2 `summary.json` 直接汇总 object/goal/joint visible、joint segdepth-valid、mask pixel min/median/max 和 occluder-active counts；合成测试 5/5 通过，真实 Push/Pick occluded 分别报告 relational-valid 66/66 与 0/72。 | 大规模采集质量门控 |
| 2026-07-16 17:45:00 UTC | Panda normalized 到 metric task action 可逆性 | 控制器源码确认 position `×0.1m`、root-aligned XYZ Euler `×-0.1rad`、gripper `[−1,1]→[0,1]`；六条 smoke 均未触发 rotation unit-ball clipping。固定 Push 71 步 metric→Panda 最大误差 `1.49e-8`，GPU7 真实 replay 1/1 final success。 | Panda 数据的跨机器人动作训练出口 |
| 2026-07-16 18:45:00 UTC | collection 训练前一致性审计 | 新的只读 audit 同时拒绝 unsuccessful/重复 simulator seed、seed/sidecar 数量或 ID 不一致、缺失 action provenance/visibility QA、LeRobot episode/condition/action 契约不一致。真实三视觉 metric smoke 通过，报告 3 unique seeds、3 episodes、202 steps。 | 外机大规模数据进入训练前的硬门控 |
| 2026-07-16 20:00:00 UTC | future target 与 policy condition 隔离 | 默认 exporter 的既有“无 future modality”断言继续通过；只有 `MANISKILL_EORT_INCLUDE_FUTURE_TARGETS=1` 且 metric action 时才写独立 `*_metric_dynamics` 视图，audit 要求 conversion manifest 显式标记 future target。真实三视觉 dynamics smoke 为 3 episodes/202 steps。 | DiT4DiT object dynamics 辅助监督数据边界 |
